<%= render 'layouts/header', header: "Bands" %>

<h4> Here are all of our bands! </h4>

<ul>
  <% @bands.each do |band| %>
    <li>
      <%= link_to band.name, band_url(band) %>
    </li>
  <% end %>
</ul>

<%= link_to "Add A Band", new_band_url %>
